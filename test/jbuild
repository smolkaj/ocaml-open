(jbuild_version 1)

(executable
 ((name test)
  (libraries (open unix))))

(alias
 ((name runtest)
  (deps (test.exe ${ROOT}/test/ocaml.pdf ${ROOT}/test/ocaml.png ${ROOT}/test/ocaml.svg))
  (action (run ${<}))
))
